[project]
name = "scratch-llm"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "numpy>=2.4.3",
    "requests>=2.33.0",
]

[tool.uv.sources]
my_local_package = { path = "./src" }
